# Email Sender Component Diagram
# C4 Level 3 - Component
# Architecture: AWS Lambda + Go
# Description: Consumes from email SQS queue, formats email content, sends via SES SendEmail API, updates delivery status in DynamoDB

direction: right

email-sender: "Email Sender" {
  tooltip: "Consumes from email SQS queue, formats email content, sends via SES SendEmail API, updates delivery status in DynamoDB"
}

# Relationships
email-sender -> notification-service/delivery-channels/ses-email-service: "sends via SES"
email-sender -> notification-service/data-store/delivery-status-table: "writes status"
