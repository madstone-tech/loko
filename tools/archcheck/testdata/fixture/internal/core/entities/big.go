package entities

// Big intentionally exceeds the 5 effective-line entity-file-size limit.

// A is a type.
type A struct{ X int }

// B is a type.
type B struct{ Y int }

// C is a type.
type C struct{ Z int }

// D is a type.
type D struct{ W int }

// E is a type.
type E struct{ V int }

// F is a type.
type F struct{ U int }
